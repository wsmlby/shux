# Copy to .env and adjust before running `docker compose up`.
# Used to set a strong session-signing secret; generate one with:
#   openssl rand -hex 32
COOKIE_SECRET=change-me-to-a-long-random-string
