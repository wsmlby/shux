{
  "name": "shux",
  "private": true,
  "version": "0.1.0",
  "description": "Shux - a self-hosted media server for digital books",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w web && npm run build -w server",
    "start": "npm run start -w server"
  }
}
